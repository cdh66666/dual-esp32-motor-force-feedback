# 断电后的连续烧录回归

- 新鲜 COM4 STATUS 母线 0.07 V，awake=0，PWM=0 后开始。
- COM4 / 68EE8F5381E4 连续三轮重刷 0.5.9-sync-trace，均经 COM19 ROM、镜像 hash 验证、应用重连、STOP/sleep 和 MODEL 版本回读成功。没有运动命令。
- 第一轮 1200 切换也出现 WinError 31，随后仍成功枚举 COM19，说明瞬时 SetCommState 错误不能单独作为失败判据。
- COM23 / 68EE8F52A79C 仍在普通 115200 串口配置时返回 WinError 31，不能回复命令，没有执行该板新一轮写入。电机供电断开不会切断 USB 对主控的供电。
- 本结果仅 COM4 三轮通过，不证明 COM23 或双板自动下载故障已解决。不再次尝试此前被拒绝的 PnP 重启，也不以别的工具重现该拒绝操作。

## Type-C 重焊后的新一轮验证

- 用户报告重新焊接 Type-C 后，两板普通状态查询各 30/30 成功；此后按用户要求对 COM23 连续执行三轮自动烧录。
- COM23 三轮均自动进入 COM18 ROM、写入 449024 字节镜像并 hash 校验成功、返回应用 COM23、回复 STOP/sleep，MODEL 回读 0.5.9-sync-trace，脚本退出码均为 0。过程中无需手动 BOOT 或拔插。
- 最终 COM4 / COM23 各再执行 30 次 STATUS，均 30/30 成功。两板固件均 0.5.9，24 V / 1.5 A / 5.2 设置保留，awake=0、PWM=0；母线分别 0.00 / 0.06 V。
- 新八字段 TRACE_META 两板均已回读，count=0，仅协议验证。
- 本次 COM23 连续三轮未复现卡死。不能据此断定唯一根因为焊点，也不是长期可靠性认证；带电整定、双板总线受力和电流尖峰仍未验收。COM4 的三轮烧录记录是前一轮，不混作重焊后的三轮。
