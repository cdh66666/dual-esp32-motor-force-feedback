# 2026-09-15 电脑与手机控制检查

## 实机状态

- COM23（68EE8F52A79C）USB双向回包通过，母线约19.55V，idle/awake0/PWM0。
- COM4（68EE8F5381E4）PnP存在，但打开仍报WinError31。经COM23查询DATA地址184也超时；不据此判定硬件损坏。已请用户按对应板RST，尚未得到复位后确认。
- COM23零PWM零点校准：校准前约137mA，唤醒校准后瞬间约-1mA；随后STOP/sleep，后续约-58mA，长期偏置/噪声未解决。没有发送位置、速度、电流或PWM运动目标，未更改控制增益。

## 已修改

- 电脑控制对象显示实际在线端口；不足两台时禁用双板选项并说明应选择哪台，不偷偷切换控制目标。
- 自动连接失败不再吞掉原因；失败重试节流5秒。
- 热点端按回包判断离线，周期重新查找常用地址，恢复后仍需显式给目标。USB占用拒绝翻译为明确中文操作提示。
- 增加电脑/手机交接候选接口：先STOP、sync off、sleep确认，再关闭串口并阻止后台自动重连/旧目标写入；显式接回电脑后仍保持停止。此状态仅保存在后台内存，后台重启需重新交接。
- 前端先探测后台能力，旧后台不支持时“交给手机”保持禁用，不把404当成功。

## 验证与部署边界

- `tests/gateway_health_test.mjs`：模拟掉线、恢复、USB拒绝不发目标、双板STOP通过。
- `tests/phone_handoff_test.py`：2项隔离测试通过，交接期间禁止重连/目标写入，未打开真实串口。
- `tests/shared_dashboard_test.cjs`：手机UA、触摸、390×844视口，真实前端配模拟接口；两台显示、开页无动作、位置滑条、双板停止、无横向溢出/JS异常通过。截图 `evidence/shared-mobile-dashboard.png` 为模拟，不是无线或实机运动验收。
- 固件编译通过，日志 `evidence/phone-health-20260915-build.log`，本轮候选固件未烧录。
- 2026-09-15 后台部署命令被工具返回 `rejected: blocked by policy`，未提供细分原因。命令整体未执行，旧后台仍服务8766；未换工具、脚本或进程入口绕过。新交接接口尚未上线，不能宣称可用。
- 电脑静态页修改已由旧后台提供并经浏览器读取验证，COM23单板选择有效。双板运动、真实手机AP登录/HTTP控制和力反馈仍未验收。

下一步：COM4物理复位后核对身份及USB/DATA回包；获可用部署路径后上线交接后台；再验证真实手机接管，不恢复历史运动目标。
