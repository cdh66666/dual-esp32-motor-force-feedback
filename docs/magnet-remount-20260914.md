# 磁铁重装后：响应优先 · 2026-09-14

用户确认可继续短时小幅测试，随后明确不必极高精度，优先快速响应和肉眼观感。未以显示滤波掩盖真实反馈，也不承诺严格零超调。

## 检查与采用配置

- 两板身份重新核对，序列号仍为 COM4 `68EE8F5381E4`、COM23 `68EE8F52A79C`。
- 六秒停止/休眠静态采样：COM4 604 帧、角度不变；COM23 602 帧、3 个原始计数峰峰值，电机轴角度约 0.07°。这不能排除旋转中的磁场、偏心、固定或传感器问题。
- COM23 上次未验收的积分衰减候选已实际回退烧录到稳定控制逻辑。COM23 原先启用基于 raw_encoder 的转子补偿 scale=1/coulomb=0.137/offset=-0.0121；磁铁重装后相位有效性无法沿用，已关闭 scale 并保存，谐波数据未清除。其失效是否为抖动根因尚未独立证明。
- 多组单纯提高/降低增益失败，未保存；较高增益可引起明显振荡，最终未采用。

| 电机轴单位配置 | COM4 | COM23 |
| --- | --- | --- |
| 速度 Kp / Ki | 0.001 / 0 | 0.0004 / 0.008 |
| 低速摩擦电流 | 0.3 A | 0 A |
| 位置 Kp / Ki / Kd | 8 / 0 / 0.1 | 12 / 0 / 0.05 |
| 位置死区 | 1° 电机轴，约 0.192° 输出轴 | 0.1° 电机轴 |
| 转子角度补偿 | 关闭 | 关闭，保留旧表 |

COM4 已烧录外环保存 v2，并返回保存成功；v2 将五个增益、摩擦电流和死区作为一个 NVS 数据块保存，兼容 v1 读取。COM23 仍使用前版五增益持久化固件，其原增益未改、禁用转子补偿已保存。
COM4 镜像 SHA256：`FECF8A1DB67BA57DEF3F746A1805C774FD99DC95D7E36DE28CDEAE5C5F1CFEEF`。
电流环、保护、力反馈控制逻辑未修改。COM4 新配置取消了速度积分，因此**带载恒速性能尚未复验，不能宣称所有模式性能保持不变**。

## 最后一次逐台实测

证据 `evidence/position-authority-live-1789367930564.json`：每台 +18°、0、−18°、0，各保持 3 秒。

| 指标 | COM4 | COM23 |
| --- | --- | --- |
| 首次进入 ±0.5° | 0.483～1.133 s | 0.234～0.660 s |
| 此后不离开 ±0.5° | 0.514～1.165 s | 0.265～0.892 s |
| 最大短暂超调 | 0.2404° | 0.9962° |
| 最大尾段平均绝对误差 | 0.1693° | 0.1287° |
| 最大尾段峰峰摆幅 | 0°（采样分辨率下） | 0.0212° |

8 步尾段定位均通过，但不是严格 0.5° 超调验收：按用户最新取向，超调仍完整记录，不再单独导致失败。角度由后轴推算，100 Hz 遥测，没有独立输出轴计量或肉眼视频验收；不外推大行程、带载、长期运行。
最后两台均 idle、awake=0、PWM=0、nFAULT=1。

## 工具变更与证据

- `check_encoder_stationary.cjs`：仅采集已停止设备，不发 wake/运动，检查身份/会话；`encoder-stationary-1789367306395.json`。
- `verify_position_authority.cjs` 默认记录超调，尾段仍须误差/峰峰值小于 0.5°；严格测试可加 `--max-overshoot=0.5`。
- `await_current_zero.cjs` 只把最长等待从 8 秒增加至 20 秒，10 样本、900 ms、均值/RMS 5 mA 和静止要求未放宽。COM23 曾多次未通过这一准备检查，最终复测前通过；不能视为零点漂移问题已彻底解决。
- `magnet-remount-com23-restore.log`、`magnet-remount-com4-tuning-v2.log`：烧录证据。
- `position-authority-live-1789367617647.json` 与 `1789367710292.json`：COM4 前两轮通过，后者 COM23 在运动前零点检查失败，不能称整轮双板通过。
- 语法与六项离线契约通过。网页/后台本轮未重启，不自动恢复旧运动。
