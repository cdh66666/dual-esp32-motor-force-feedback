# 双板短脉冲辨识：阶段记录，尚未整定通过

## 当前实机条件

按用户最新说明，电机连接、20 V / 3 A 电源供电、已加保护罩。保留每板 1.5 A 设置、5.2 减速比。本阶段未重新烧录，未启动力反馈。

## 本轮实际执行

- 0.5.9 同步电流/编码器记录已经取得。150 mA 较长脉冲与 300 mA 短脉冲的数据不能支撑现有机械模型。
- 改用 200/225/250 mA、中间档较长脉冲，COM4 34.21°、COM23 25.97° 触发既有 25° 位移保护。未放宽该阈值，回退原参数。
- 一次将板端命令租期误设为 80 ms，低于固件的 100 ms 下限。现已修正，新增下限回归检查；异常采样不再覆盖原始命令错误。
- 最终采用主机 80 ms 采集窗口、板端 100 ms 租期，每板完成 12 段 200/225/250 mA 正反向激励；均未触发位移保护。实际同步记录约 88 ms，不能把主机定时当作精确的激励时长。

## 已保存的原始数据

- evidence/sync-identify-COM4-20260908.json 与 COM23 同名记录。
- evidence/midrange-identify-COM4-20260908.json 与 COM23 同名记录。
- evidence/bounded-identify-COM4-20260908.json 与 COM23 同名记录，最终每板 12 段。

## 离线结论与边界

使用末尾两段完整脉冲作为留出集，避免同一段相邻样本泄漏进训练/验证两边。机械差分窗口改为 20 ms，仍排除停滞和反转区间，保留 25% 留出误差标准。

- 电气回归留出误差：COM4 9.13%，COM23 13.97%。有效电感包含闭环滤波/延迟，不能当作精密绕组电感；拟合通过不等于新电流 PI 实测通过。
- COM4 机械模型留出误差 68.0%，不通过。COM23 留出机械样本不足，不通过。
- COM4 两段同为 +200 mA、约 88 ms：输出角位移约 0.731° 与 12.638°。这是实测的响应不一致，不足以单独证明齿槽、摩擦、负载或编码器中哪项是根因。
- 未采用任何新的三环参数。未完成带载/位置超调/双板力反馈验收，不宣称最佳性能。

## 软件修改与验证

- 异常脉冲保留原始错误和已有遥测，再尝试收齐高速记录；采样不足也保存记录。
- 完整脉冲留出集包含训练/验证样本数和组号，方便审查。
- identification_split_test、three_loop_model_test、moving_electrical_model_test、current_trace_metrics_test、auto_tune_cancel_test、force_lifecycle_contract_test 通过；server_control_contract_test 17 项通过。
- tests/bounded_identification_replay.cjs 可离线重现上述拒绝结论，不执行电机命令。

下一步应先区分静止起动与持续运动的模型适用区间，检验角度/方向相关扰动，再对可验证的电流 PI 与外环分别进行受限复测；不能继续只增加脉冲次数或放松验证标准。

## 后续同轮：计算电流 PI 已进行独立实测

整定代码调整为先计算及验证电气模型对应的电流 PI，再尝试机械模型和外环。新增 tools/validate_saved_current.cjs，核对硬件序列号、固件版本、原配置一致性，复用已保存的辨识数据，仅执行正负 150 mA 短时验证；每板结束后停止并恢复原电流 PI。

实测证据：evidence/saved-current-validation-1788871547448.json。

- COM4：正向均值 149.85 mA，跟踪 RMS 5.14 mA；负向均值 -150.79 mA，跟踪 RMS 3.34 mA。
- COM23：正向均值 147.06 mA，跟踪 RMS 7.27 mA；负向均值 -149.49 mA，跟踪 RMS 5.26 mA。
- 四段均满足既有短脉冲电流验收标准；记录最大采样间隔 1.88–2.01 ms。
- 测量范围仅为 ±150 mA、约 180 ms 短脉冲的末段 60 ms、滤波反馈电流；不代表 PWM 载波纹波、最大电流或持续负载能力。
- 两板均 ACK 恢复 kp=600、ki=600000、max_pwm=4095，最终状态 idle、PWM0、nFAULT1，电源约 19.65/19.72 V。不把本次原参数恢复写成已永久采用新 PI。
