# 用户更正物理测试条件

用户在后续消息明确说明：之前被描述为“电机电线断开”的带电烧录，实际电机仍连接；用户观察未转动并使用了隔离防护。

此前执行时依据的是用户当时明确给出的“电机连接驱动板的电线插头已拔下”。原始日志中的 ISOLATED_POWERED_TEST 仅记录当时的前提声明，不是传感器验证的事实。保留原始日志并以本更正说明其适用性。

可确认：烧录、镜像校验、应用重连和回读成功；用户报告没有观察到异常。不能确认：复位和下载阶段驱动输出无瞬态，或所有故障情况下可带电连接电机烧录。当前应按电机已连接处理，不能再使用 --motor-leads-disconnected 选项。

同轮整定流程优化：将计算增益后的 650 ms 电流复测改为 ±150 mA、180 ms、板端 220 ms 期限，读取 RAM 高速记录；最后 60 ms 检查目标误差与波动，所有样本检查绝对峰值，拒绝采样空洞与不足。保持原 25° / 0.8 A 边界，不作为连续负载或 PWM 纹波验收。软件及旧实测回放测试通过；新实机整定尚未执行。本轮未烧录。
