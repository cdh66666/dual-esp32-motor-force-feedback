# 分方向电气模型检查

工具 `tools/audit_directional_model.cjs`，记录 `evidence/directional-model-1788882336431.json`。仅离线分析，没有修改板端参数或启动运动。

训练来自 bounded-identify 两板旧批次，每个方向保留末两条完整脉冲作为旧批次留出数据；系数仅用剩余训练脉冲拟合。额外检查 saved-current-validation-1788881164154.json 的完整独立脉冲，各档预测误差不超过25%。该独立批次已经用于探索模型选择，若选中候选，仍必须另采前瞻实机验证，不能把它冒充全新未见测试集。

| 方向 | 独立误差范围 | 结论 |
|---|---:|---|
| COM4正 | 8.4–17.7% | 本离线检查通过，不等于可采用 |
| COM4反 | 8.3–31.1% | -100mA超标，压降系数-.0959V也超出既定容许范围 |
| COM23正 | 20.8–29.0% | +150mA超标 |
| COM23反 | 6.4–20.2% | 本离线检查通过，不等于可采用 |

分方向建模不能使所有方向和电流档通过。不能只选择通过方向宣称两板模型成功，不能改变原25%门槛掩盖未通过方向，也不能把COM4不符合物理范围的系数写入。

下一阶段应改进辨识实验覆盖和可重复性，而不是反复用这批短脉冲挑选模型。需要在可控起点和速度段采集，区分静止电气响应、运动反电动势、方向压降与起动阻力；继续使用整组设置/回退和独立新批次验收。
