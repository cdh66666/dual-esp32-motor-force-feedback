# 共用通道与 COM4 检查

- 示波器位置/速度在快照层由输出轴度转换为 r / r/s；不改变控制数据或串口单位。游标、纵轴和图例使用相同快照。
- 一组位置/速度/电流/PWM 勾选同时控制两个窗格。滑条位置标签改为 r。
- 用户明确点击连接时，健康端口仍按正常连接处理；已打开但无遥测/回执失败的端口改用 recover-link 重建句柄，失败显示错误，不再静默假连接。后台自动连接逻辑未扩大恢复动作。
- COM4 枚举存在，USB 父设备 PnP 状态 OK，但 100 多秒无 RX，model 回执超时。显式重建串口后仍无数据，现有 DTR 回退也未恢复。不能由此断定 Type-C 焊点故障；待用户重新插拔该板 USB。COM23 遥测正常。
- scope_contract_test、quick_control_test、connect_recovery_contract 验证单位换算、原数据不变、共用通道和连接路由。无固件修改、无电机运动指令。
