# 成组接口服务端部署与实时兼容性验证

2026-09-08 核实端口 8766 进程 117176 命令行为本项目 web/server.py，先逐板 STOP、sync off、status 确认 idle/PWM0，再仅重启该服务。新进程 89140。连接已自动恢复，COM4/COM23 USB 身份分别为 68EE8F5381E4、68EE8F52A79C，遥测新鲜，USB problem 列表为空。

实际 `cascade electrical` 查询已通过新服务校验并送达两板；两板旧固件返回 ERR usage，服务报告 error_kind=controller，而非传输故障。未发送任何候选配置，未启动运动。重连后的两板状态为 awake=0、PWM0、idle，母线约 19.65/19.74 V。

新固件未烧录。当前电机接线和供电均在，现有身份锁定烧录工具只支持 VM<1 V 或用户已实际拔下电机接线的隔离供电测试。历史更正明确电机目前连接，不能使用 --motor-leads-disconnected 声称已经隔离，也没有绕过该检查执行复位。

需要物理配合后才能使用当前烧录流程：关闭电机电源，USB 保持连接。软件侧接口编译、服务部署和旧固件拒绝分类已验证，不替代新固件板端设置/回退验收。
