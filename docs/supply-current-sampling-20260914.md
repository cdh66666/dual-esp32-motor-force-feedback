# 现有供电适配与电流采样一致性

本记录优先于同日较早的调参记录。用户要求尽可能少转动测试；该要求到达后已结束当前测试、STOP/SLEEP 两板，未再启动运动。后续只做代码检查、编译、保存参数、烧录与静止回读。

## 采用的工作档

位置参考速度上限 8 r/s（不是瞬时实测速度保证），加速度 18000 电机轴 deg/s²，输出位置范围仍为 ±10 圈。8 r/s 仅限制位置参考，不改变手动速度目标范围；速度 PI 参数与手动速度模式共用，该模式尚未单独验收。已验收的力反馈控制律与参数未修改。运行强度低于原 15 r/s 档，是适应现有可调电源的明确折中，不宣称功率系统问题已根治。

| 参数，电机轴单位 | COM4 | COM23 |
|---|---:|---:|
| 速度 Kp / Ki | 0.001 / 0.001 | 0.0004 / 0.008 |
| 摩擦前馈 A | 0.3 | 0 |
| 位置 Kp / Ki / Kd | 8 / 0 / 0.15 | 9 / 0 / 0.3 |
| 死区，电机轴度 | 1 | 1 |
| 外环电流上限 A | 1.2 | 1.2 |

独立外环参数用已存在的 `cascade save_outer` 保存。位置速度/加速度工作档写入 36GP 默认值，重启不回到原 15 r/s 档。电流 PI 600/600000、方向、极性和力反馈参数不变。

## 已有实测依据与限制

- `full-range-retarget-1789369982777.json`：两板 8 r/s 档逐台完成 ±10 圈及连续改目标，无本次测试电压/电流边界中止。COM23 旧参数短行程仍有回摆。
- COM4 采用组：`full-range-retarget-1789370122832.json` 中的 COM4；+10 到 -10 圈首次进入 ±0.5°为 3917 ms，完整静态目标最大观测超调 0.383°。一个 +2 圈动作 3 秒末误差 0.652°，不能称所有精度达标。该文件中的 COM23 候选组更差，未采用。
- COM23 采用组：`full-range-retarget-1789370296781.json`；+10 到 -10 圈首次进入 ±0.5°为 3831 ms，+2 圈动作最大超调 2.758°、3 秒末误差 1.167°、末 500 ms 摆幅 1.473°。多数较长保持动作尾段摆幅较小，但仍不是完全不抖或全部 ±0.5°合格。
- 同参数照搬失败组 `full-range-retarget-1789370212131.json` 已回退，没有采用。
- 采用组最低采样母线分别 19.06 V、18.80 V；峰值采样电流分别 1.271 A、1.346 A。100 Hz 记录不能证明没有更快的电流/过压尖峰。
- 两板响应时间是逐台测得，尚未做同时运动验收。`verify_paired_position.cjs` 仅已编写，未运行；用户要求减少转动后不补跑。

## 电流滤波审查

现有实现已包含四次 ADC 转换平均，以及运动时 tau=1.52205 ms 的一阶低通。由参数解析计算截止频率约 104.6 Hz、阶跃到 90%约 3.50 ms；这是滤波器解析值，不是实机带宽测量。仅增大低通时间常数会增加闭环延迟，不能据网页抖动就认定应加重滤波。

修正 DATA `busSendStatus()`：活动控制中复用已有角度、母线和电流快照，不再额外读 I2C/ADC 并推进共享滤波器。滤波系数、电流保护和网页原始曲线不变。此缺陷是代码证据，不是已证实的全部抖动根因。

`tests/current_sampling_contract.cjs` 及 8 项离线检查通过。该改动没有新增运动验证，抖动改善尚不能单独归因于它。

固件 SHA256：`AA4C707E29D06304CBBD1B72A89F1F073CBD8BDA72CED39D64CDDA1291F8264B`。部署日志：`evidence/supply-sampling-com4-20260914.log`、`evidence/supply-sampling-com23-20260914.log`。最终回读状态以日志为准。

后续优先使用已保存数据进行离线回放，明确候选修改再安排一次必要的短验证，不再轮番全行程扫参数。尚未完成独立 USB 网关、板端 Wi-Fi 热点或全部工况验收。
